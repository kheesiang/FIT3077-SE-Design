<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cholesterol">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UserInterfaces.BPMonitorActivity"></activity>
        <activity android:name=".graphs.BPGraphActivity" />
        <activity android:name=".graphs.CholGraphActivity" />
        <activity android:name=".UserInterfaces.MonitorActivity"
                  android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".UserInterfaces.MainActivity"
                  android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>